working with the branches
using Modules how we work in projects
we can use branches in Git.

#in local system developer working
import random

number = random.randint(1, 100)
guess = None

while guess != number:
    guess = int(input("Guess a number between 1 and 100: "))
   
    if guess < number:
        print("Too low!")
		#end of random code
		